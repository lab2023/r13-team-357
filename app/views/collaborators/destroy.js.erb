$("#collaborators").html("<%= escape_javascript( render(partial: 'collaborators', collection:  @current_project.users, as: :user) ) %>");
$("#collaborator_form").html("<%= escape_javascript( render(partial: 'form') ) %>");


$("#collaborator_user_id").autocomplete({
    source: $('#collaborator_user_id').data('autocomplete-source'),
    autoFocus: true
});