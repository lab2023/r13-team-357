- @current_project.users.each do |user|
  - if current_user != user
    %p
      = "#{user.email} - #{user.name}"
      = link_to 'Delete', collaborator_path(@current_project, email: user.email), method: :delete, remote: true