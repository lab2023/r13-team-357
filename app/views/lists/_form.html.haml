.panel.panel-default
  .panel-heading
    %i.icon-edit.icon-large
    = yield :form_title
  .panel-body
    = simple_form_for(@list) do |f|
      = f.error_notification

      .form-inputs
        = f.input :name
        = f.input :sort
        = f.association :project

      .form-actions
        = f.button :submit, class: 'btn btn-default'
        = link_to t('cancel'), lists_path, class: 'btn'
